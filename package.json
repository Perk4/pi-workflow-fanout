{
  "name": "pi-workflow-fanout",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js fixture/clamp.test.js",
    "workflow": "node run.js",
    "coach": "node run.js --live --approve",
    "check:live": "node scripts/check-live.js",
    "check:local": "node scripts/check-local.js"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "pi-extensible-workflows": "5.14.0"
  }
}
